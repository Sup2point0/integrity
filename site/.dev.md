## LaTeX

```math
\textcolor{#4d9dcd}{blue}
```


## Import Order

```ts
// stylesheets
import "#styles/";

// modules (external)
import { feature } from "external";

// modules (special)
import { one_off } from "./";

// modules (internal)
import { store } from "#scripts/";
import { feature } from "#scripts/";
import { utility } from "#scripts/utils/";
import type { utility } from "#scripts/types/";

// components (special)
import OneOff from "./";

// components (core)
import { Component } from "#parts/ui";

// modules (Svelte/Kit)
import { onMount } from "svelte";
import { base } from "$app/paths";
```


## Dynamic Scriptures

- Use ***triple emphasis*** for keywords
