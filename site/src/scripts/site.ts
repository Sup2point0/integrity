// import { data } from "./data";
import type { QuestionsData } from "#scripts/types";


interface SiteData {
  questions: QuestionsData;
}

const Site: SiteData = {
  // questions: data,
};
export default Site;
